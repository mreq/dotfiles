# Installation

```
sudo apt install git

git clone git@github.com:mreq/dotfiles.git ~/.local/share/dotfiles

sudo apt update && sudo apt install -y $(jq -r '.apt[].package' ~/.local/share/dotfiles/setup/packages.json)

~/.local/share/dotfiles/config/install.sh
~/.local/share/dotfiles/setup/setup.sh

pip3 install $(jq -r '.pip[].package' ~/.local/share/dotfiles/setup/packages.json) --break-system-packages

sudo ln -s ~/.local/share/dotfiles/cron/clear_downloads /etc/cron.daily/clear_home_petr_downloads

sudo ln -s ~/.local/share/dotfiles/cron/clear_dropbox /etc/cron.daily/clear_home_petr_dropbox

sudo ln -s ~/.local/share/dotfiles/cron/clear_work_logs /etc/cron.daily/clear_home_petr_work_logs
```
