#!/usr/bin/env python3
import i3
import sys
import os

if i3.filter(focused=True, nodes=[])[0]['window_properties']['class'] == sys.argv[1]:
  i3.command('workspace back_and_forth')
else:
  if i3.focus(clasS=sys.argv[1]) != True:
    os.system('sh -c ' + sys.argv[2])
