[engine]
# Use podman-compose instead of docker-compose so `podman compose` calls podman
# directly rather than going through the docker API. Avoids tag normalization
# (docker.io/library/ prefix) that breaks Dev Containers' localhost/ lookup.
compose_providers = ["/home/petr/.config/containers/podman-compose-host.sh"]
