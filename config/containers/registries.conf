# Devcontainer-friendly short-name resolution:
# resolve `ruby:3.3.3-bookworm` etc. against docker.io silently,
# instead of prompting to pick between Fedora/Red Hat/Docker Hub.
unqualified-search-registries = ["docker.io"]
short-name-mode = "permissive"
