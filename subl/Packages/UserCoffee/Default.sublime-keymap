[
  // Ember: go to - find and open coffee/emblem file with matching file
  {
    "keys": [" ", "e", "g"],
    "command": "run_cmd",
     "args": {
       "command": "fn=$file_name; fn=${fn##*/}; pt -g=\".*${fn%.*}\\.(coffee|em).*\" | grep -v $fn | grep -v tests | xargs subl"
     },
    "context": [{ "key": "vi_command_mode_aware" }]
  }
]
