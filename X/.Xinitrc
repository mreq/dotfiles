#!/bin/sh
if [ -d /etc/x11/xinit/xinitrc.d ]; then
  for f in /etc/x11/xinit/xinitrc.d/*; do
    [ -x "$f" ] && . "$f"
  done
  unset f
fi

xrdb -merge ~/.Xdefaults

xmodmap ~/.Xmodmap

xflux -l 50.0350280 -g 14.2343540

unagi

i3

true
